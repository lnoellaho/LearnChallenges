<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1> List of Teams</h1>
  <% for(var key in responses) { %>
    <div>
      Team: <%= responses[key].teamname %>
      <br />
      Players: <%= responses[key].players %>
      <br / />
      Wins: <%= responses[key].wins %>
      <br />
      Loses: <%= responses[key].loses %>
      <br />
      <br />
  </div>
  <% } %>
    <a href="/maketeam"> Back</a>
  </body>
</html>
